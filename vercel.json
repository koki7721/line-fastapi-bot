{
  "version": 2,
  "builds": [
    {
      "src": "API/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/",         "dest": "API/main.py" },       // ← 追加
    { "src": "/api/(.*)", "dest": "API/main.py" }        // ← 既存
  ]
}
